This program converts the rather obscure TKL GPS trace format into the much more portable GPX format.

The TKL format is used by:
	* SOMA watches
	* Ultrasport Navrun

to build:
	make

to run:
	java Tkl2Gpx <tklfile>

will output
	<tklfile>.gpx
